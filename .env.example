# SmartClipboard Local Environment Overrides
# Copy this file to '.env' and modify the values for your local development setup.

# Your Apple Development Code Signing Identity (find via `security find-identity -v -p codesigning`)
# For local unsigned/ad-hoc builds, you can leave it as "-" or comment this out.
SMARTCLIPBOARD_SIGN_IDENTITY="Apple Development: saihgupr@icloud.com (W3GLW5YKN6)"

# The bundle identifier of the application
SMARTCLIPBOARD_BUNDLE_ID="com.saihgupr.SmartClipboard"
